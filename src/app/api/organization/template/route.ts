import {requirePermission} from "@/lib/authorization";import {styledSheet,workbookResponse} from "@/lib/excel";import * as XLSX from "xlsx";import {NextRequest} from "next/server";
const config={position:{module:"positions",filename:"jabatan",sheet:"Jabatan",headers:["Kode Jabatan","Nama Jabatan"]},department:{module:"departments",filename:"departemen",sheet:"Departemen",headers:["Kode Departemen","Nama Departemen"]},branch:{module:"branches",filename:"cabang",sheet:"Cabang",headers:["Kode Cabang","Nama Cabang","Alamat","Radius Absensi","Zona Waktu"]}} as const;
export async function GET(request:NextRequest){try{const kind=request.nextUrl.searchParams.get("kind") as keyof typeof config,item=config[kind];if(!item)throw new Error();await requirePermission(item.module,"create");const workbook=XLSX.utils.book_new(),example=kind==="position"?["KASIR","Kasir"]:kind==="department"?["OPS","Operasional"]:["SMR-01","Samarinda Utama","Jl. Contoh",100,"Asia/Makassar"];XLSX.utils.book_append_sheet(workbook,styledSheet([[...item.headers],example],item.headers.map(()=>24)),item.sheet);XLSX.utils.book_append_sheet(workbook,styledSheet([["Petunjuk","Keterangan"],["Kode","Wajib dan unik dalam perusahaan"],["Nama","Wajib; minimal 2 karakter"],["Catatan","Gunakan sheet DATA untuk import massal"]],[24,64]),"PETUNJUK");return workbookResponse(workbook,`template-${item.filename}-panboy-hr.xlsx`);}catch{return new Response("Tidak diizinkan",{status:403});}}
