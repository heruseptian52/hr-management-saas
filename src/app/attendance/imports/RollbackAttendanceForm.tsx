"use client";export function RollbackAttendanceForm({batchId,filename}:{batchId:string;filename:string}){return <form action="/api/attendance/import/rollback" method="post" onSubmit={e=>{if(!confirm(`Rollback data BARU dari ${filename}? Data lama yang diperbarui tidak akan dihapus.`))e.preventDefault();}}><input type="hidden" name="batchId" value={batchId}/><button className="danger-button">Rollback</button></form>}
