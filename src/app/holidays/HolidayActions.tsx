"use client";
export function HolidayActions({id,name,active,canEdit,canDelete}:{id:string;name:string;active:boolean;canEdit:boolean;canDelete:boolean}){return <div className="toolbar-actions">{canEdit&&<form action="/api/holidays" method="post"><input type="hidden" name="action" value="TOGGLE"/><input type="hidden" name="id" value={id}/><button>{active?"Nonaktifkan":"Aktifkan"}</button></form>}{canDelete&&<form action="/api/holidays" method="post" onSubmit={e=>{if(!confirm(`Yakin hapus permanen hari libur ${name}?`))e.preventDefault();}}><input type="hidden" name="action" value="DELETE"/><input type="hidden" name="id" value={id}/><button className="danger-button">Hapus</button></form>}</div>}
