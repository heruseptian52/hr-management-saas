"use client";
import {useState} from "react";
type Row={row:number;code:string;name:string;startTime:string;endTime:string;breakMinutes:number;lateToleranceMin:number;minStaff:number;maxStaff:number|null;color:string;isActive:boolean;status:"NEW"|"UPDATE"|"ERROR";errors:string[]};type Preview={filename:string;rows:Row[];summary:{total:number;new:number;update:number;error:number}};
export function ShiftImportDialog(){const[open,setOpen]=useState(false),[busy,setBusy]=useState(false),[preview,setPreview]=useState<Preview|null>(null),[result,setResult]=useState<{created:number;updated:number}|null>(null);async function upload(file:File){setBusy(true);const body=new FormData();body.set("file",file);const response=await fetch("/api/shifts/import/preview",{method:"POST",body}),json=await response.json();setBusy(false);if(!response.ok)return alert(json.error);setPreview(json);}async function commit(){if(!preview)return;setBusy(true);const response=await fetch("/api/shifts/import/commit",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({filename:preview.filename,rows:preview.rows.filter(row=>row.status!=="ERROR").map(({status,errors,row,...data})=>data)})}),json=await response.json();setBusy(false);if(!response.ok)return alert(json.error);setResult(json);}function close(){setOpen(false);setPreview(null);setResult(null);if(result)location.href="/shifts?saved=import";}return <><button type="button" onClick={()=>setOpen(true)}>Import Excel</button>{open&&<div className="modal-backdrop"><section className="import-dialog"><header><div><b>Import Shift</b><small>Upload → Preview → Konfirmasi</small></div><button onClick={close}>×</button></header>{!preview&&!result&&<div className="import-stage"><h3>Upload Excel Shift</h3><p>Gunakan template agar format jam dan kapasitas terbaca dengan benar.</p><input type="file" accept=".xlsx,.xls" disabled={busy} onChange={event=>event.target.files?.[0]&&void upload(event.target.files[0])}/><a href="/api/shifts/template">Download Template</a></div>}{preview&&!result&&<div className="import-stage"><h3>Preview Import</h3><div className="preview-summary"><b>Total<span>{preview.summary.total}</span></b><b>Baru<span>{preview.summary.new}</span></b><b>Update<span>{preview.summary.update}</span></b><b>Error<span>{preview.summary.error}</span></b></div><div className="preview-table">{preview.rows.slice(0,200).map(item=><div key={item.row}><span>{item.row}</span><strong>{item.code} — {item.name}<small>{item.startTime}–{item.endTime}</small></strong><b className={`row-${item.status.toLowerCase()}`}>{item.status}</b><small>{item.errors.join("; ")}</small></div>)}</div><p>Kode yang sudah ada akan diperbarui. Baris error tidak dimasukkan.</p><button onClick={()=>setPreview(null)}>Ganti File</button><button disabled={busy||preview.summary.new+preview.summary.update===0} onClick={()=>void commit()}>{busy?"Mengimport...":`Konfirmasi ${preview.summary.new+preview.summary.update} Data`}</button></div>}{result&&<div className="import-stage"><h3>Import Selesai</h3><div className="preview-summary"><b>Baru<span>{result.created}</span></b><b>Update<span>{result.updated}</span></b></div><button onClick={close}>Lihat Master Shift</button></div>}</section></div>}</>}
