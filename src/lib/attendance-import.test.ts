import { describe,expect,it } from "vitest";import { attendanceDate,attendanceStatus,attendanceTime,parseAttendanceWorkbook } from "./attendance-import";import * as XLSX from "xlsx";
describe("attendance import",()=>{it("maps Indonesian headers",()=>{const book=XLSX.utils.book_new();XLSX.utils.book_append_sheet(book,XLSX.utils.aoa_to_sheet([["NAMA KARYAWAN","TANGGAL","JAM MASUK"],["Heru","14-09-2026","08:00"]]),"DATA");const parsed=parseAttendanceWorkbook(XLSX.write(book,{type:"buffer",bookType:"xlsx"}));expect(parsed.suggestedMapping).toMatchObject({employee:"NAMA KARYAWAN",workDate:"TANGGAL",checkIn:"JAM MASUK"});});it("normalizes statuses",()=>{expect(attendanceStatus("HADIR")).toBe("PRESENT");expect(attendanceStatus("Sakit")).toBe("SICK");});it("validates times",()=>{expect(attendanceTime("8:05").value).toBe("08:05");expect(attendanceTime("25:00").warning).toContain("tidak valid");});it("reads Indonesian dates",()=>{expect(attendanceDate("14 SEPTEMBER 2026").date?.toISOString().slice(0,10)).toBe("2026-09-14");});});
